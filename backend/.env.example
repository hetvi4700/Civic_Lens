# Copy to .env and fill in your values:
#   cp .env.example .env

MONGODB_URI=mongodb://127.0.0.1:27017
DB_NAME=civic_lens
PORT=5001

# Comma-separated allowed browser origins (default: http://localhost:5173)
CORS_ORIGIN=http://localhost:5173

# Only required for NYC Open Data download / preprocessing scripts (not for Box demo)
APP_TOKEN=your_nyc_open_data_app_token
